name: GA2 Action

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1002778@ds.study.iitm.ac.in
        run: echo "Hello, world!"
